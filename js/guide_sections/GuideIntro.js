var React = require('react/addons');

var GuideIntro = React.createClass({

  render() {
    return (
      <p>
        Index for mobile and presentation or index for desktop?
      </p>
    );
  }

});

module.exports = GuideIntro;